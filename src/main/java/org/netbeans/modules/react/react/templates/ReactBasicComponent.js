/**
 *
 * @author ${user}
 * Date: ${date}, ${time}
 */
import React, {PropTypes} from 'react';

class ${name} extends React.Component {
    render() {
    
    }    
}

${name}.propTypes = {};
${name}.defaultProps = {};

export default ${name};